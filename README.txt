//READ ME

// This mini app is a test if I can itirate through an array with tuples and get data from it. 

// All answers are hidden until the answer button is pressed. 

// Next button will being the next question. Once it reaches the max, it will return to 0. 